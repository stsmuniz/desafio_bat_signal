import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    padding: 10,
    borderColor: '#fee202',
    borderWidth: 2,
    borderRadius: 5,
    backgroundColor: '#fff',
    marginVertical: 10,
  },
});